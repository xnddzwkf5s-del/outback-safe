📀 OUTBACK-SAFE USB — OFFLINE AI SURVIVAL ASSISTANT

┌─────────────────────────────────────────────────────┐
│ ⚠️ ALWAYS AVAILABLE: outback-safe/index.html       │
│    Open in any browser. 142 survival pages.         │
│    No Python. No install. Always works.             │
└─────────────────────────────────────────────────────┘

─── macOS ───
1. Open 'ai' folder
2. Double-click 'start.command' (or 'Start AI.app')
3. Wait ~3-5s for model to load
4. Browser opens automatically

─── Windows ───
⚠️ Python 3.10+ must be installed:
   https://www.python.org/downloads/
   (Check "Add Python to PATH")

Option A (PowerShell):
1. Open 'ai' folder, right-click 'start.ps1'
2. "Run with PowerShell"
3. Wait ~15-45s for model to load

Option B (Command Prompt):
1. Open 'ai' folder, double-click 'start.bat'
2. If SmartScreen blocks: "More info" → "Run anyway"
3. Wait ~15-45s for model to load

─── EMERGENCY (both OS) ───
Open 'outback-safe/index.html' in any browser
📞 000 (Australia)

📁 ADD YOUR OWN KNOWLEDGE
   Drop .md files in 'content/' folder
   Format: +++ frontmatter +++ then markdown body

─── ADDING NEW CONTENT (both OS) ───
1. Drop .md files into the 'content/' folder
2. Run the reindex script:
   macOS:   double-click ai/reindex.command
   Windows: double-click ai/reindex.bat
3. Restart the AI (start.command / start.bat)
